import React from "react";

import AddStudentForm from "../billing/add-student";

export default function StudentTable({query} : {query: string}) {

    
  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Add Student to Customer</h2>
    </div>
  );
}